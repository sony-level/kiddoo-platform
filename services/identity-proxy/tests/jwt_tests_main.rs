/**
 * JWT Tests - Main Entry Point
 *
 * This test suite is organized into modules:
 * - test_helpers: Common utilities and test data creation
 * - verification_tests: Basic JWT verification scenarios
 * - expiration_tests: Token expiration handling
 * - security_tests: Signature validation and algorithm checks
 * - caching_tests: JWKS key caching behavior
 * - jwks_tests: JWKS endpoint and response handling
 * - role_extraction_tests: Role extraction from claims
 */
mod jwt;
