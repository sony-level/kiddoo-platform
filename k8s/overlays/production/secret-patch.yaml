apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: kiddoo-secrets
spec:
  refreshInterval: 30m
  secretStoreRef:
    name: aws-secrets-manager
    kind: ClusterSecretStore
  data:
    - secretKey: database-url
      remoteRef:
        key: kiddoo/production/database-url
    - secretKey: oauth-client-id
      remoteRef:
        key: kiddoo/production/oauth-client-id
    - secretKey: oauth-client-secret
      remoteRef:
        key: kiddoo/production/oauth-client-secret
    - secretKey: jwt-secret
      remoteRef:
        key: kiddoo/production/jwt-secret
