# =============================================================================
# GitHub Actions — PR Validation (before merge)
#
# Role: Fast feedback on PRs. Validates code quality before merge.
# CD pipeline (cd.yml) handles: build, Docker, SonarQube, Trivy, deploy.
# deploy-test.yml handles: on-demand test environment.
#
# Mirrors local pre-commit hooks: fmt, clippy, tests, secret scan.
# See docs/environments.md for full environment strategy.
# =============================================================================
name: PR Validation

on:
  push:
    branches:
      - "feature/*"
      - "bugfix/*"
      - "hotfix/*"
      - "*"
  pull_request:
    branches:
      - "develop"
      - "release"
      - "release/*"
      - "master"

env:
  CARGO_TERM_COLOR: always

jobs:
  # -------------------------------------------------------------------
  # Lint: format + clippy
  # -------------------------------------------------------------------
  lint:
    name: Lint
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy, rustfmt

      - name: Cache cargo registry
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-lint-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-lint-

      - name: Check formatting
        run: cargo fmt --all -- --check

      - name: Run Clippy
        run: cargo clippy --workspace -- -D warnings

  # -------------------------------------------------------------------
  # Unit + integration tests (lightweight)
  # -------------------------------------------------------------------
  test:
    name: Test
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:16-bookworm
        env:
          POSTGRES_DB: kiddoo_test
          POSTGRES_USER: kiddoo
          POSTGRES_PASSWORD: kiddoo_test_pwd
        ports:
          - 5433:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    env:
      DATABASE_URL: postgres://kiddoo:kiddoo_test_pwd@localhost:5433/kiddoo_test
      ENVIRONMENT: test

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Cache cargo registry
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-test-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-test-

      - name: Install Diesel CLI
        run: cargo install diesel_cli --no-default-features --features postgres

      - name: Run migrations
        run: cd libs/database && diesel migration run

      - name: Run tests
        run: cargo test --workspace --verbose

  # -------------------------------------------------------------------
  # Secret scan (prevent accidental credential leaks)
  # -------------------------------------------------------------------
  secret-scan:
    name: Secret Scan
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Validate env files have no real secrets
        run: |
          echo "Checking .env.* files for real secrets..."
          FOUND=0
          for f in .env.development .env.test .env.staging .env.production; do
            if [ -f "$f" ]; then
              # Check for values that don't look like placeholders
              if grep -P '(SECRET|PASSWORD|TOKEN)=(?!<from-)(?!\$\{).{8,}' "$f" | grep -v '#' | grep -v 'placeholder' | grep -v 'example'; then
                echo "::warning::Potential real secret in $f"
                FOUND=1
              fi
            fi
          done
          if [ "$FOUND" -eq 1 ]; then
            echo "::error::Real secrets detected in tracked .env files!"
            exit 1
          fi
          echo "✓ No real secrets in .env files"

      - name: Run Gitleaks
        uses: gitleaks/gitleaks-action@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  # -------------------------------------------------------------------
  # Quick build validation (compile check only)
  # -------------------------------------------------------------------
  build-check:
    name: Build Check
    runs-on: ubuntu-latest
    needs: [lint, test]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Cache cargo registry
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-build-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-build-

      - name: Build check
        run: cargo build --workspace --release

  # -------------------------------------------------------------------
  # Discord notification (PR validation result)
  # -------------------------------------------------------------------
  notify:
    name: Discord Notify
    runs-on: ubuntu-latest
    needs: [lint, test, secret-scan, build-check]
    if: always()

    steps:
      - name: Send Discord notification
        env:
          DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK_URL }}
        run: |
          if [ "${{ needs.build-check.result }}" = "success" ] && \
             [ "${{ needs.lint.result }}" = "success" ] && \
             [ "${{ needs.test.result }}" = "success" ] && \
             [ "${{ needs.secret-scan.result }}" = "success" ]; then
            COLOR=3066993
            STATUS="PR Validation PASSED"
          else
            COLOR=15158332
            STATUS="PR Validation FAILED"
          fi

          PAYLOAD=$(cat <<EOF
          {
            "embeds": [{
              "title": "$STATUS",
              "description": "**${{ github.repository }}**\nPR: #${{ github.event.pull_request.number }} — ${{ github.event.pull_request.title }}\nBranch: \`${{ github.head_ref }}\` → \`${{ github.base_ref }}\`\nAuthor: ${{ github.actor }}",
              "url": "${{ github.event.pull_request.html_url }}",
              "color": $COLOR,
              "footer": { "text": "GitHub Actions — PR Validation" },
              "timestamp": "$(date -u +%Y-%m-%dT%H:%M:%SZ)"
            }]
          }
          EOF
          )

          curl -s -H "Content-Type: application/json" -d "$PAYLOAD" "$DISCORD_WEBHOOK"
