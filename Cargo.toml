
[workspace]  # Workspace for baby sitting service
members = [
    "libs/database", "libs/metrics", "services/api-gateway", "services/identity-proxy", "services/orchestrator",
]
resolver = "2"
default-run = "orchestrator"

[workspace.package] # Configuration for all workspace members
description = "Baby sitting service API"
authors = ["Level Sony"]
version = "0.1.0"
edition = "2024"
